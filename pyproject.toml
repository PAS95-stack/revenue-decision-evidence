[project]
name = "revenue-decision-evidence"
version = "0.3.1"
description = "Deterministic, source-row-traceable revenue decision evidence workflow"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
